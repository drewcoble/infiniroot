// Same values as infinidraft's own constants/general.ts - the two apps'
// BottomNav components float the same size pill in the same spot, so there's
// no reason for these to drift.
export const BOTTOM_NAV_BOTTOM_OFFSET = 7;
export const BOTTOM_NAV_HEIGHT = 58;
