# Same Convex deployment infinifaab (the web app) uses - see
# ../infinifaab/.env.local.example. Copy this file to .env.local and set
# the value from `npx convex dev` (run from ../infinidata).
EXPO_PUBLIC_CONVEX_URL=
